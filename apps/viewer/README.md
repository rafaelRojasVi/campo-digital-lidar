# viewer (placeholder)

No application code yet. This will eventually be a web-based point-cloud /
mesh viewer for reviewing ROI selections and volume-estimation results.

Explicitly out of scope for this bootstrap: React frontend, 3D web
rendering stack, auth. See `docs/architecture.md` for the intended future
stack.
